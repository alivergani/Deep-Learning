# Lettura e manipolazione dati
pandas>=2.0
numpy>=1.24
tables>=3.8            # backend HDF5 per pandas.read_hdf
h5py>=3.8              # ispezione a basso livello dei file .h5
pyarrow>=14.0          # backend Parquet
PyYAML>=6.0

# Analisi e modelli
scikit-learn>=1.3      # StratifiedKFold, roc_curve, roc_auc_score
scipy>=1.10            # fit del fondo dijet, statistica
tensorflow>=2.15

# Grafici e notebook
matplotlib>=3.7
jupyterlab>=4.0

# --- opzionali, solo per l'estensione sui costituenti grezzi ---
# pyjet>=1.8           # clustering anti-kT dei costituenti
# energyflow>=1.3      # utility per jet e observables
